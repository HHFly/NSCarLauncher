<resources>

    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>

    </style>
    <style name="AppTheme" parent="BaseAppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="Theme.AppLauncher" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@mipmap/ic_launcher</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="BaseLine"></style>

    <style name="BaseLine.Horizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/view_line_width</item>
        <item name="android:background">@color/view_line</item>
    </style>

    <style name="BaseLine.Horizontallist">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/view_line_width</item>
        <item name="android:background">@color/view_line</item>
        <item name="android:layout_marginLeft">38px</item>
    </style>

    <style name="BaseLine.Vertical">
        <item name="android:layout_width">@dimen/view_line_width</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/view_line</item>
    </style>

    <style name="BaseDialogAnim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_push_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_push_out</item>
    </style>
    <style name="mystyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter</item>
        //进入时的动画
        <item name="android:windowExitAnimation">@anim/dialog_exit</item>
        //退出时的动画
    </style>
    <style name="BaseText"></style>
    <style name="BaseText.Tv">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:includeFontPadding">false</item>
    </style>


    <style name="nodarken_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground"> @android:color/transparent </item>
        <item name="android:backgroundDimEnabled">false</item>

    </style>

    <style name="dialog_localmusic_style" parent="android:Animation">


    </style>
    <!--Theme.Dialog||Theme.Holo.Light.Dialog-->
    <style name="dialog_lhp" parent="@android:style/Theme.Holo.Light.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:background">#00000000</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!--自定义switch的按钮和轨迹颜色theme-->
    <style name="mySwitch" parent="Theme.AppCompat.Light">
        <!-- switch 打开时的按钮的颜色 轨迹颜色默认为30%（看效果就明白30%是怎么回事了）这个颜色 -->
        <item name="colorControlActivated">@color/green</item>
        <!--  switch关闭时的按钮的颜色 -->
        <item name="colorSwitchThumbNormal">@color/white</item>
        <!-- switch关闭时的轨迹的颜色 30%这个颜色 -->
        <item name="android:colorForeground">@color/colormusicblue</item>

    </style>
    <declare-styleable name="RulerView">
        <attr name="titleName" format="reference|string" />
        <attr name="unitName" format="reference|string" />
        <attr name="minValue" format="reference|integer" />
        <attr name="maxValue" format="reference|integer" />
        <attr name="currentValue" format="reference|integer" />
    </declare-styleable>
</resources>
